services:
  fastapi:
    build:
      context: .
      dockerfile: backend/docker/Dockerfile
    image: fastapi_backend
    container_name: fastapi_dev
    volumes:
      - ./.env:/app/.env:ro
    environment:
      - PYTHONPATH=/app/src
    ports:
        - "8000:8000"